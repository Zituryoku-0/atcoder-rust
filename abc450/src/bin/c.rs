use proconio::input;
fn main() {
    input! {
        h: u32,
        w: u32,
        multi_arrays: [[char; w]; h]
    }
    print!("{}", n);
    print!("{:?}", arrays);
    print!("{:?}", multi_arrays);
}
